[build]
builder = "dockerfile"

[deploy]
startCommand = "python manage.py migrate && gunicorn inventory_management.wsgi:application --bind 0.0.0.0:$PORT --access-logfile - --error-logfile -"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10
